import UuidContent from '@/components/Uuid/UuidContent';

export const metadata = {
  title: 'UUID Generator & Validator || TheHTTP',
  description: 'Generate UUIDs v4 and validate UUID strings with ease',
};

export default function UuidPage() {
  return <UuidContent />;
}
